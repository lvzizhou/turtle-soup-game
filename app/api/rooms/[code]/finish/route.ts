import { NextResponse } from 'next/server'; import { finishRoom } from '@/lib/store'; import { loadRoom } from '@/lib/room-loader'; import { supabase } from '@/lib/supabase'; export async function POST(req:Request,{params}:{params:{code:string}}){try{const restored=await loadRoom(params.code);if(!restored)throw Error('房间不存在');const {playerId}=await req.json(); const room=finishRoom(params.code,playerId);if(process.env.NEXT_PUBLIC_SUPABASE_URL){const {error}=await supabase.from('rooms').update({status:'finished'}).eq('room_code',params.code);if(error)throw error;}return NextResponse.json({room});}catch(e:any){return NextResponse.json({error:(e as any).message},{status:400});}}
